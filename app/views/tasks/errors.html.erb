<% if @task.errors.any? %>
    <div class="col-lg-12" >
        <div class="col-lg-4 mx-auto" style="position:relative; top:30vh;">
            <div class="card" style=" background:yellow;">
                <div class="card-body">
                    <h2><%= "kindly fix the errors" %></h2>
                    <p class="lead" style="color:red"><%= @task.errors.full_messages %></p>
                    <%= link_to 'back', new_task_path, class: 'btn btn-outline-primary' %>
                </div>
            </div>
        </div>
    </div>

    <div class="circle">
        <div class="circle2"></div>
    </div>
<%end%>